[project]
name = "itewe"
version = "0.1.0"
description = "DataTrove-native OCR pipeline for processing documents using consumer hardware"
readme = "README.md"
authors = [
    { name = "Ogundepo Odunayo", email = "ogundepoodunayo@gmail.com" },
    { name = "Akintunde `theyorubayesian` Oladipo", email = "akintunde.oladipo@taresco.org" }
]

requires-python = ">=3.12"
dependencies = [
    "datatrove[cli,io,processing]>=0.8.0",
    "azure-storage-blob>=12.0.0",
    "pyyaml>=6.0.0",
    "orjson>=3.11.7",
    "pypdf>=6.6.2",
    "aiofiles>=25.1.0",
    "pyarrow>=23.0.0",
    "datasets>=4.5.0",
    "aiosqlite>=0.22.1",
    "hydra-core>=1.3.2",
    "xgboost>=3.2.0",
    "pymupdf==1.26.1",
]

[project.optional-dependencies]
vllm = [
    "vllm>=0.6.0",
]

exo = [
    "exo>=0.1.0",
]

ollama = [
    "ollama>=0.1.0",
]

mlx = [
    "mlx>=0.1.0",
    "mlx-lm>=0.1.0",
]

all = [
    "itewe[vllm,exo,ollama,mlx]",
]
lid = [
    "cleantext>=1.1.4",
    "emoji>=2.15.0",
    "fasttext>=0.9.3",
    "fasttext-numpy2-wheel>=0.9.2",
]
azure = [
    "adlfs>=2026.2.0",
]

[project.scripts]
itewe = "itewe:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
asyncio_mode = "auto"

# Bandit security checker configuration
[tool.bandit]
exclude_dirs = ["tests", ".venv", "venv"]
skips = ["B101", "B601"]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "ipywidgets>=8.1.8",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.0.0",
    "ruff>=0.15.2",
]
